[project]
name = "task-manager"
version = "0.1.0"
description = "Task Manager OpenEnv project"
authors = ["Akash Rao"]
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"